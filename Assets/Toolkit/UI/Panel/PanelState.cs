
namespace Toolkit.UI.PanelSystem {
    public enum PanelState {
        None,
        Showing,
        Hiding,
        Ready,
        // Temporary state to handle destruction
        Destroyed,
    }
}
