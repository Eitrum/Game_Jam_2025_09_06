namespace Toolkit {
    /// <summary>
    /// Used to handle some attributes to deal with how it should be rendered.
    ///   - ButtonAttribute
    ///   - ReadonlyAttribute
    /// </summary>
    public enum EditorGUIMode {
        Default,
        EditorOnly,
        RuntimeOnly,
    }
}
